# --- JWT ---
jwt.secret=troque-por-uma-string-aleatoria-longa-de-pelo-menos-32-caracteres
jwt.expiration-ms=3600000
jwt.refresh-expiration-ms=604800000

# --- Google OAuth (login social) ---
# Deixe vazio por enquanto se ainda não tem client-id configurado no Google Cloud Console
google.client-id=

# --- Mercado Pago ---
mercadopago.access-token=TEST-0000000000000000-000000-00000000000000000000000000000000-000000000
mercadopago.base-url=https://api.mercadopago.com

# --- Preços das assinaturas ---
subscription.price.monthly=29.90
subscription.price.annual=299.00

# --- Banco de dados MySQL ---
spring.datasource.url=jdbc:mysql://localhost:3306/evermynd?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=P@ssw0rd
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

# --- Flyway ---
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

#extr
spring.jpa.properties.hibernate.type.preferred_uuid_jdbc_type=CHAR